<div class="audioIndex">
<h1 class="title is-1">List of Audiobooks</h1>

<div class="columns has-text-centered is-centered is-multiline">

<% audiobooks.forEach(audiobook => { %>
  <div class="column is-one-quarter">
  <a href="/audiobooks/<%= audiobook.id %>">
  <img class="bookCoverIndex" src=<%= audiobook.bookCover %> alt<%= audiobook.name %> />
  <h2 class="title is-2"><%= audiobook.name %></h2>
  </a>
  </div>
<% }) %>

</div>
</div>
